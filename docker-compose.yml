services:
  omnia-gui:
    build:
      context: .
      dockerfile: apps/gui/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - GOOGLE_API_KEY=${GOOGLE_API_KEY:-}
    volumes:
      - omnia-data:/app/apps/gui/data

volumes:
  omnia-data:
